# datapod-for-react-go-json

This is a simple datapod template for a local site that displays data and uses a Go script to parse a text file into JSON data.

# features

-   dataModel has async functions even though it loads directory from JSON
    -   this allows you to later add async data loading, e.g. from an external API or external JSON file
